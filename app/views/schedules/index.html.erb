<div class="calendar_container">
	<div id="calendar">
	</div>
</div>

#Code to add
<%= month_calendar cars: @schedules do |date, schedules| %>
<%= date %>

<% schedules.each do |schedule| %>
<div>
	<%= schedule.start_time %>
</div>
<% end %>

<% end %>