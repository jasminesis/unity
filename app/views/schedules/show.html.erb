<div class="col-8">

	<h1 class="display-4"><%= @schedule.event %></h1>

	<p>
		<%= @schedule.start_time %>

		<% if @schedule.users.length > 0 %>

		with

		<% @schedule.users.each do |users| %></a>
		<%= users.username %>
		<% end %>

		<% end %>
	</p>
</div>