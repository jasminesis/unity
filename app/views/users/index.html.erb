<h1>List of Users</h1>
<td>
	<% @users.each do |user| %>
		<li>
			<% if user.online_status == 'true' %>
				<div class="online"></div>
			<% end %>
			<%= link_to user.username, user_path(user)%>
		</li>
	<% end %>
</td>